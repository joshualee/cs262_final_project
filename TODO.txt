javadoc comments

when a client unregisters with sever, inform other clients?

synchronization concerns:
  what happens when a client registers while a vote is going on?
    don't allow new clients to register?

how to gracefully abort vote when a client unregisters during voting

general exception handling and making sure functions only throw the exceptions they should